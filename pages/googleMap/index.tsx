import React from 'react';
import GoogleMap from '@Components/GoogleMap';

const index = () => {
    return (
        <div>
            <GoogleMap></GoogleMap>
        </div>
    );
};

export default index;
